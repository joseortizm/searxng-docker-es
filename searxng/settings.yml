# see https://docs.searxng.org/admin/settings/settings.html#settings-use-default-settings
use_default_settings: true
server:
  # base_url is defined in the SEARXNG_BASE_URL environment variable, see .env and docker-compose.yml
  secret_key: "b281650f444145c538898e3c1edb804491daf5178fedce1048e65aef76cbec49"  # change this!
  limiter: false  # enable this when running the instance for a public usage on the internet
  image_proxy: true
redis:
  url: redis://redis:6379/0

# sin formats por defecto devuelve html. Si queremos explicitamente que devuelva json y html
# https://docs.searxng.org/admin/installation-searxng.html
search:
  formats:
    - json 
    - html
